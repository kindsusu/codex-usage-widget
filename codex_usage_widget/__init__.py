"""Codex usage widget package."""

from typing import Final

#: Bump together with ``pyproject.toml`` and the git tag. The release workflow
#: refuses to publish unless all three agree, and the auto-updater compares this
#: value against the latest release tag.
__version__: Final = "2.0.2"
